# AgentDisk 人工测试计划

## 测试环境准备

### 前置条件

| 服务 | 端口 | 启动命令 |
|------|------|----------|
| MySQL 8.0 | 3306 | 已部署 |
| MinIO | 9000 | 已部署 |
| Redis（可选） | 6379 | `redis-server` |
| 后端 API | 9100 | `make run` |
| 测试网关 | 3000 | `cd gateway && npm run dev` |
| Web 前端 | 5173 | `cd web && npm run dev` |

### 配置确认

1. `config.yaml` 中 `oauth2.enabled` 设为 `true`
2. `oauth2.auth_url` 指向 `http://localhost:3000/oauth2/authorize`
3. `oauth2.redirect_url` 指向 `http://localhost:5173/auth/callback`
4. 环境变量 `DB_PASSWORD`、`OSS_ACCESS_KEY`、`OSS_SECRET_KEY` 已设置

---

## 测试用例

### T1: OAuth2 登录流程

**目标**：验证用户通过测试网关 OAuth2 登录后能正常访问 AgentDisk

| 步骤 | 操作 | 预期结果 | 实际结果 | 通过 |
|------|------|----------|----------|------|
| T1.1 | 打开浏览器，访问 http://localhost:5173 | 浏览器被重定向到 `http://localhost:3000/login` 网关登录页 | | |
| T1.2 | 输入用户 ID `user001`，密码 `test123`，点击「登录」 | 登录成功，页面短暂显示网关授权确认页（或直接跳转） | | |
| T1.3 | （如出现授权页）点击「允许」 | 浏览器重定向回 `http://localhost:5173`，显示 AgentDisk 主界面 | | |
| T1.4 | 观察页面顶部栏 | 显示应用标题「AgentDisk」，右侧显示用户 ID `user001` 和存储空间用量条 | | |
| T1.5 | 点击右侧用户下拉菜单中的「退出登录」 | 页面跳转到登录页，再次访问 5173 需重新登录 | | |

### T2: 文件夹管理

**目标**：验证文件夹的创建、浏览和删除

| 步骤 | 操作 | 预期结果 | 实际结果 | 通过 |
|------|------|----------|----------|------|
| T2.1 | 登录后，点击「新建文件夹」按钮 | 弹出「新建文件夹」对话框，父文件夹 ID 默认为 0 | | |
| T2.2 | 输入文件夹名称「测试文件夹A」，点击「创建」 | 对话框关闭，文件列表中出现文件夹「测试文件夹A」，类型显示「文件夹」 | | |
| T2.3 | 点击「测试文件夹A」名称 | 页面导航到 `/explorer/{id}`，显示空文件列表 | | |
| T2.4 | 在子文件夹中再创建一个文件夹「子文件夹B」 | 创建成功，列表显示「子文件夹B」 | | |
| T2.5 | 点击浏览器后退，或点击面包屑「全部文件」 | 返回根目录，列表中仍可见「测试文件夹A」 | | |
| T2.6 | 点击「测试文件夹A」右侧的「删除」按钮 | 弹出确认对话框「确定删除文件夹？...」 | | |
| T2.7 | 确认删除 | 文件夹从列表中消失 | | |

### T3: 文件上传

**目标**：验证文件上传功能

| 步骤 | 操作 | 预期结果 | 实际结果 | 通过 |
|------|------|----------|----------|------|
| T3.1 | 在根目录，点击「上传文件」按钮，选择一个小于 10MB 的文件（如 txt 或 png） | 文件开始上传 | | |
| T3.2 | 等待上传完成 | 页面显示「xxx 上传成功」提示，文件出现在列表中 | | |
| T3.3 | 验证列表中文件信息 | 文件名正确，大小合理，类型显示正确 | | |
| T3.4 | 点击「上传文件」选择一个 .md 文件上传 | 上传成功，列表中出现 md 文件 | | |
| T3.5 | 点击「上传文件」选择一个代码文件（如 .go / .py / .ts）上传 | 上传成功 | | |

### T4: 文件预览

**目标**：验证不同文件类型的在线预览

| 步骤 | 操作 | 预期结果 | 实际结果 | 通过 |
|------|------|----------|----------|------|
| T4.1 | 点击列表中 .md 文件的名称 | 页面切换到预览模式，Markdown 正确渲染（标题、列表、代码块等） | | |
| T4.2 | 点击预览页的「返回」按钮 | 返回文件列表 | | |
| T4.3 | 点击代码文件的名称 | 页面显示代码高亮预览，带行号 | | |
| T4.4 | 点击图片文件的名称 | 页面显示图片预览 | | |
| T4.5 | 点击 txt 文件的名称 | 页面显示纯文本内容 | | |
| T4.6 | 点击列表中文件的「操作」→「预览」 | 同 T4.1-T4.5 效果 | | |

### T5: 文件下载

**目标**：验证文件下载流程

| 步骤 | 操作 | 预期结果 | 实际结果 | 通过 |
|------|------|----------|----------|------|
| T5.1 | 点击某文件的「操作」→「下载」 | 浏览器打开新标签页，开始下载文件 | | |
| T5.2 | 检查下载的文件 | 文件名和大小与上传时一致 | | |

### T6: 文件删除与回收站

**目标**：验证文件删除和回收站恢复/彻底删除

| 步骤 | 操作 | 预期结果 | 实际结果 | 通过 |
|------|------|----------|----------|------|
| T6.1 | 点击某文件的「操作」→「删除」 | 弹出确认对话框 | | |
| T6.2 | 确认删除 | 文件从列表中消失，提示「已移至回收站」 | | |
| T6.3 | 点击侧边栏「回收站」 | 页面显示回收站列表，刚才删除的文件在其中 | | |
| T6.4 | 点击该文件的「恢复」按钮 | 提示「已恢复」，文件从回收站消失 | | |
| T6.5 | 返回文件列表 | 被恢复的文件重新出现在列表中 | | |
| T6.6 | 再次删除该文件，进入回收站，点击「彻底删除」 | 弹出确认「确定彻底删除？此操作不可恢复」 | | |
| T6.7 | 确认彻底删除 | 文件从回收站消失，存储空间用量更新 | | |

### T7: 标签管理

**目标**：验证标签绑定、解绑和搜索

| 步骤 | 操作 | 预期结果 | 实际结果 | 通过 |
|------|------|----------|----------|------|
| T7.1 | 点击某文件的「操作」→「标签」 | 弹出标签管理弹窗 | | |
| T7.2 | 输入标签名「重要」，点击「添加」 | 标签「重要」出现在标签列表中，提示「标签已添加」 | | |
| T7.3 | 再添加标签「文档」 | 标签列表显示两个标签 | | |
| T7.4 | 关闭弹窗，回到文件列表 | 文件行的「标签」列显示「重要」「文档」 | | |
| T7.5 | 点击侧边栏「标签搜索」 | 进入标签搜索页面 | | |
| T7.6 | 输入「重要」，点击搜索 | 搜索结果列表中出现刚才标记的文件 | | |
| T7.7 | 输入「重要,文档」，点击搜索 | 搜索结果同时包含两个标签的文件 | | |
| T7.8 | 回到文件操作→标签，点击「重要」标签的 x 号 | 标签移除，提示「标签已移除」 | | |

### T8: 版本历史与回滚

**目标**：验证文件版本管理和回滚

| 步骤 | 操作 | 预期结果 | 实际结果 | 通过 |
|------|------|----------|----------|------|
| T8.1 | 上传一个测试文件 test.txt（内容为 "version 1"） | 上传成功 | | |
| T8.2 | 点击该文件「操作」→「版本历史」 | 右侧弹出抽屉，显示版本列表，包含 v1 | | |
| T8.3 | 通过「操作」找到文件的更新功能，上传同名文件（内容为 "version 2"） | 文件更新成功 | | |
| T8.4 | 再次查看版本历史 | 列表显示 v1 和 v2 两个版本 | | |
| T8.5 | 点击 v1 的「回滚」按钮 | 弹出确认 | | |
| T8.6 | 确认回滚 | 提示「已回滚到 v1」 | | |

### T9: 分享管理

**目标**：验证创建分享、查看分享列表、公开访问分享

| 步骤 | 操作 | 预期结果 | 实际结果 | 通过 |
|------|------|----------|----------|------|
| T9.1 | 点击某文件的「操作」→「分享」 | 弹出创建分享弹窗 | | |
| T9.2 | 输入提取码「abc123」，最大访问次数 10，有效期 72 小时，点击「创建分享」 | 提示成功 | | |
| T9.3 | 点击侧边栏「我的分享」 | 分享列表中出现刚才创建的分享记录，显示分享码、提取码、访问次数等信息 | | |
| T9.4 | 复制分享码 | 复制成功 | | |
| T9.5 | 打开新的无痕浏览器窗口，访问 `http://localhost:5173/share/{分享码}` | 显示分享访问页面，要求输入提取码 | | |
| T9.6 | 输入提取码「abc123」，点击「访问」 | 验证成功，显示文件信息和「下载文件」按钮 | | |
| T9.7 | 点击「下载文件」 | 文件开始下载 | | |
| T9.8 | 回到原浏览器「我的分享」页面 | 访问次数变为 1/10 | | |
| T9.9 | 点击该分享的「撤销」按钮 | 确认后分享状态变为「已失效」 | | |
| T9.10 | 再次在无痕窗口访问分享链接 | 提示分享已失效或无法访问 | | |

### T10: 权限管理

**目标**：验证授予和撤销 Agent 权限

| 步骤 | 操作 | 预期结果 | 实际结果 | 通过 |
|------|------|----------|----------|------|
| T10.1 | 点击侧边栏「权限管理」 | 显示权限列表页面，当前为空或显示已有权限 | | |
| T10.2 | 点击「授予权限」按钮 | 弹出授权表单 | | |
| T10.3 | 输入 Agent ID `agent-test-001`，资源 ID 选择一个文件 ID，资源类型选「文件」，权限选「Read」 | 填写完成 | | |
| T10.4 | 点击「授予」 | 提示「权限已授予」，列表中出现新记录 | | |
| T10.5 | 验证列表中的记录 | 显示 Agent ID、资源 ID、类型 Tag(file)、权限 Tag Read(蓝色) | | |
| T10.6 | 点击该记录的「撤销」按钮 | 确认后权限从列表中消失 | | |

### T11: 存储空间显示

**目标**：验证空间用量条正确反映使用量

| 步骤 | 操作 | 预期结果 | 实际结果 | 通过 |
|------|------|----------|----------|------|
| T11.1 | 观察顶部栏右侧的空间用量条 | 显示「已用空间 / 总空间」和百分比进度条 | | |
| T11.2 | 上传一个较大文件 | 进度条百分比增加，已用空间数值增加 | | |
| T11.3 | 删除文件并彻底删除 | 进度条百分比减少 | | |

### T12: 测试网关管理

**目标**：验证网关仪表盘功能

| 步骤 | 操作 | 预期结果 | 实际结果 | 通过 |
|------|------|----------|----------|------|
| T12.1 | 访问 http://localhost:3000/dashboard | 显示网关仪表盘，包含当前用户信息、快速操作、OAuth2 配置、测试用户列表 | | |
| T12.2 | 查看 OAuth2 配置区域 | 显示 Client ID、各端点 URL 等配置信息 | | |
| T12.3 | 在用户管理区域，输入新用户 ID/名称/密码，点击「添加」 | 用户添加成功，列表更新 | | |
| T12.4 | 点击某用户的「删除」按钮 | 确认后用户从列表中移除 | | |
| T12.5 | 点击「打开 AgentDisk」按钮 | 新标签页打开 `http://localhost:5173` | | |
| T12.6 | 访问 http://localhost:3000/oauth2/debug | 显示 OAuth2 调试页面，可配置参数发起授权请求 | | |

### T13: 跨用户隔离

**目标**：验证不同用户之间的数据隔离

| 步骤 | 操作 | 预期结果 | 实际结果 | 通过 |
|------|------|----------|----------|------|
| T13.1 | 用 user001 登录，创建文件夹「张三专属」并上传一个文件 | 操作成功 | | |
| T13.2 | 退出登录 | 回到登录页 | | |
| T13.3 | 用 user002 登录（`user002` / `test123`） | 登录成功 | | |
| T13.4 | 查看文件列表 | 列表为空，看不到 user001 的任何文件和文件夹 | | |

### T14: 页面导航与响应式

**目标**：验证侧边栏导航和基本响应式表现

| 步骤 | 操作 | 预期结果 | 实际结果 | 通过 |
|------|------|----------|----------|------|
| T14.1 | 依次点击侧边栏：全部文件 → 回收站 → 我的分享 → 标签搜索 → 权限管理 | 每次点击正确切换到对应页面 | | |
| T14.2 | 点击「全部文件」回到主页 | 正确显示文件列表 | | |
| T14.3 | 缩小浏览器窗口到 768px 以下 | 侧边栏自动折叠 | | |
| T14.4 | 点击汉堡菜单或触发展开 | 侧边栏展开显示菜单 | | |

---

## 测试结果汇总

| 用例 | 描述 | 通过/失败 | 备注 |
|------|------|-----------|------|
| T1 | OAuth2 登录流程 | | |
| T2 | 文件夹管理 | | |
| T3 | 文件上传 | | |
| T4 | 文件预览 | | |
| T5 | 文件下载 | | |
| T6 | 文件删除与回收站 | | |
| T7 | 标签管理 | | |
| T8 | 版本历史与回滚 | | |
| T9 | 分享管理 | | |
| T10 | 权限管理 | | |
| T11 | 存储空间显示 | | |
| T12 | 测试网关管理 | | |
| T13 | 跨用户隔离 | | |
| T14 | 页面导航与响应式 | | |

**测试人**：___________ **测试日期**：___________

**总体结论**：通过 / 有条件通过 / 不通过

**问题清单**：

| 编号 | 严重程度 | 问题描述 | 复现步骤 |
|------|----------|----------|----------|
| B1 | | | |
| B2 | | | |
| B3 | | | |
